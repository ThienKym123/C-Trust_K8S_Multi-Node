apiVersion: v1
kind: PersistentVolume
metadata:
  name: mypv
spec:
  storageClassName: standard
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany  
  nfs:
    path: /mnt/nfs_share/  
    server: ${CONTROL_PLANE_IP}  
  persistentVolumeReclaimPolicy: Retain  